// Gemini API Configuration Example
// IMPORTANT: Copy this file to config.js and add your actual API key
// NEVER commit config.js to Git!

// Get your API key from: https://makersuite.google.com/app/apikey
const GEMINI_CONFIG = {
    apiKey: 'YOUR_API_KEY_HERE', // Replace with your actual API key
    model: 'gemini-pro',
    temperature: 0.7,
    maxOutputTokens: 2048,
    topK: 40,
    topP: 0.95
};

// Optional: Override in localStorage for runtime configuration
if (typeof window !== 'undefined') {
    window.GEMINI_CONFIG = GEMINI_CONFIG;
}